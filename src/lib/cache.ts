import {
  unstable_cache as cache,
  revalidateTag,
  revalidatePath,
} from "next/cache";

/**
 * Cache configuration and helper functions.
 * 
 * Use tags and paths defined here for fine-grained cache invalidation.
 */
